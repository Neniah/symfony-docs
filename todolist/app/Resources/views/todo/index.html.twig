{% extends 'base.html.twig' %}

{% block body %}
  <table class="table table-striped">
    <h2 class="page-header">Lastest Todos</h2>
    <thead>
      <tr>
        <th>#</th>
        <th>Todo</th>
        <th>Due Date</th>
      </tr>
      </thead>
      <tbody>
        <tr> <th scope="row">1</th>
          <td>NAME</td>
          <td>DUE DATE</td>
          <td><a href="{{path('todo_details', {id: 1})}}" class="btn btn-info">View</a>
              <a href="{{path('todo_edit', {id: 1})}}" class="btn btn-success">Edit</a>
              <a href="{{path('todo_delete', {id: 1})}}" class="btn btn-warning">Delete</a>
          </td>
        </tr>
        </tbody>
      </table>
{% endblock %}
